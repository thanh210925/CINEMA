@model CINEMA.Models.Room

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Sửa phòng chiếu";
}

<div class="container mt-4">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-warning text-white fw-semibold">
            <i class="bi bi-pencil-square"></i> @ViewData["Title"]
        </div>

        <div class="card-body">
            @await Html.PartialAsync("_RoomForm", Model)
        </div>

        <div class="card-footer text-center">
            <a href="/Room/Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Quay lại danh sách
            </a>
        </div>
    </div>
</div>
